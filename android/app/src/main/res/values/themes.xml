<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme: a Material3 theme so the app can boot before Compose takes over. -->
    <style name="Theme.AtlasServidor" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/fundo</item>
        <item name="android:navigationBarColor">@color/superficie</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
